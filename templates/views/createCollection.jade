
extends ../layouts/default.jade

block head
    style.
        body {
            background-color: #f6f6f6;
        }

        .grey-page-mark {
            background-color: #f6f6f6;
        }

        .top-search-form {
            display: none;
        }

        .main-search-form input {
            width: 60%;
            margin-left: 0;
            background-color: white;
            padding: 14px 16px 13px 32px;
        }

        .main-search-form .submit {
            padding: 12px 21px;
        }

        .main-search-form label {
            color: #7F8C8D;
        }

        .search-section {
            height: 60px;
            padding: 20px;
            margin: 0 auto;
            width: 60%;
            margin-bottom: 40px;
            text-align: center;
        }

block breadcrumbs
    ul
        li.inactive
            span.fa.fa-search
            |  Create Collection

block content
  div.submission-container(align='center')
    form(name="advancedSearch", method="post")

        if errors.length > 0
            div.alert.alert-danger
                each error in errors
                    p #{error}
                    br
        
        div.clearfix

        div.form-group-50.required
            label Id
            br
            input(name='metaId',placeholder='Collection Id',value=collectionMeta.id,style="width: 350px;")

        div.form-group-50.required
            label Version
            br
            input(name='metaVersion',placeholder='Version',value=collectionMeta.version,style="width: 350px;")

        div.form-group-100.required
            label Name
            br
            input(name='metaName',placeholder='Collection Name',value=collectionMeta.name,style="width: 700px;")

        div.form-group-100.required
            label Description
            br
            textarea(name='metaDescription',placeholder='Describe your collection',style="width: 700px;")
                    | #{collectionMeta.description}

        div.clearfix

        div.form-group-50.required
            table.table.advanced-search-table
                th Property
                th Value
                tr
                    td Object Type
                    td
                       select(name='objectType' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for objectType in objectTypes
                                option(selected=(objectType.name==initObjectType)) #{objectType.name}
                tr
                    td Creator
                    td
                       select(name='creator' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for creator in creators
                                option(selected=(creator.name==initCreator)) #{creator.name}
                tr
                    td Created After
                    td
                        input(type='text',name='createdAfter',placeholder='YYYY-MM-DD',value=createdAfter)
                tr
                    td Created Before
                    td
                        input(type='text',name='createdBefore',placeholder='YYYY-MM-DD',value=createdBefore)
                tr
                    td Modified After
                    td
                        input(type='text',name='modifiedAfter',placeholder='YYYY-MM-DD',value=modifiedAfter)
                tr
                    td Modified Before
                    td
                        input(type='text',name='modifiedBefore',placeholder='YYYY-MM-DD',value=modifiedBefore)
                tr
                    td ID/Name/Description
                    td
                        input(type='text',name='description',placeholder='Search terms',value=description,style="width: 550px;")
                tr
                    td Type
                    td
                        select(name='type' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for t in types
                                option(selected=(t.name==initType)) #{t.name}
                tr
                    td Role
                    td
                        select(name='role' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for role in roles
                                option(selected=(role.name==initRole)) #{role.name}
                tr
                    td Collections
                    td
                        select(name='collections' id='collections' multiple data-placeholder='Select Some Collections').chosen-select
                            for collection in collections
                                if initCollections && initCollections.includes(collection.name) 
                                    option(selected=true) #{collection.name}
                                else 
                                    option #{collection.name}
                tr
                    td
                        select(name='searchPred1' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for predicate in predicates
                                option(selected=(predicate.name==initPred1)) #{predicate.name}
                    td
                        button(type='submit',name="adv",value="Filter1").btn.btn-primary.btn-sm
                            | Filter
                        if objects1.length > 0
                            select(name='filterObj1' style='width: 260px;').chosen-select
                                option #{'No Filter'}
                                for object in objects1
                                    option(selected=(object.name==initObj1)) #{object.name}
                        else
                            input(type='text',name='searchObj1',placeholder='Search term',value=searchObj1,style="width: 500px;")
                tr
                    td
                        select(name='searchPred2' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for predicate in predicates
                                option(selected=(predicate.name==initPred2)) #{predicate.name}
                    td
                        button(type='submit',name="adv",value="Filter2").btn.btn-primary.btn-sm
                            | Filter
                        if objects2.length > 0
                            select(name='filterObj2' style='width: 260px;').chosen-select
                                option #{'No Filter'}
                                for object in objects2
                                    option(selected=(object.name==initObj2)) #{object.name}
                        else
                            input(type='text',name='searchObj2',placeholder='Search term',value=searchObj2,style="width: 500px;")
                tr
                    td
                        select(name='searchPred3' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for predicate in predicates
                                option(selected=(predicate.name==initPred3)) #{predicate.name}
                    td
                        button(type='submit',name="adv",value="Filter3").btn.btn-primary.btn-sm
                            | Filter
                        if objects3.length > 0
                            select(name='filterObj3' style='width: 260px;').chosen-select
                                option #{'No Filter'}
                                for object in objects3
                                    option(selected=(object.name==initObj3)) #{object.name}
                        else
                            input(type='text',name='searchObj3',placeholder='Search term',value=searchObj3,style="width: 500px;")
                tr
                    td
                        select(name='searchPred4' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for predicate in predicates
                                option(selected=(predicate.name==initPred4)) #{predicate.name}
                    td
                        button(type='submit',name="adv",value="Filter4").btn.btn-primary.btn-sm
                            | Filter
                        if objects4.length > 0
                            select(name='filterObj4' style='width: 260px;').chosen-select
                                option #{'No Filter'}
                                for object in objects4
                                    option(selected=(object.name==initObj4)) #{object.name}
                        else
                           input(type='text',name='searchObj4',placeholder='Search term',value=searchObj4,style="width: 500px;")
                tr
                    td
                        select(name='searchPred5' style='width: 260px;').chosen-select
                            option #{'No Filter'}
                            for predicate in predicates
                                option(selected=(predicate.name==initPred5)) #{predicate.name}
                    td
                        button(type='submit',name="adv",value="Filter5").btn.btn-primary.btn-sm
                            | Filter
                        if objects5.length > 0
                            select(name='filterObj5' style='width: 260px;').chosen-select
                                option #{'No Filter'}
                                for object in objects5
                                    option(selected=(object.name==initObj5)) #{object.name}
                        else
                            input(type='text',name='searchObj5',placeholder='Search term',value=searchObj5,style="width: 500px;")
 
        div.clearfix

        br
        br

        button(type='submit').btn.btn-primary.btn-lg
            | Create Collection




