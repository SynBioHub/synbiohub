// import changeURIPrefix from './change-uri-prefix'

jest.mock('../java', () => jest.fn())

describe('changeURIPrefix', () => {
  it('should return the ???', () => {
    expect(1).toEqual(1)
  })
})
